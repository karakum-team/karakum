name: Publish

on:
  push:
    branches:
      - master
#    tags:
#      - 'v*'

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
      - uses: ./.github/workflows/ci.yml

      - run: echo "Publish"
